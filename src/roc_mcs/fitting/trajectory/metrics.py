import numpy as np


def roughness(x):
    """
    Шероховатость траектории. Вычисляется как сумма квадратов первых разностей:

        Σ (x[i+1] - x[i])²

    Меньшие значения соответствуют более гладким траекториям.

    Параметры
    ----------
    x : array-like
        Последовательность значений параметра.
    """
    return np.sum(np.diff(x) ** 2)


def mean_roughness(x):
    """
    Средняя шероховатость траектории. Среднее квадратов первых разностей:
        mean[(x[i+1] - x[i])²]
    """
    return np.mean(np.diff(x) ** 2)